# NavaApp

A clean, focused React Native starter project.

## Getting Started

1. Install dependencies:
   npm install
2. Start the development server:
   npm start
3. To build an APK, use EAS Build or Expo build service.
